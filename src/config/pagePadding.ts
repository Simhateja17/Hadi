// Centralized page top padding configuration (in pixels)
// Adjust these numbers to change the space between the navbar and page content

export const PAGE_PADDING_TOP = {
  gallery: 80,
  contactus: 64,
  toolkits: 80,
  blogs: 64,
  aboutus: 64,
} as const;


// Hi